"""
hr_assistant.ui — Streamlit UI components for the Agentic HR Portal.

Modules:
    styles           — Global CSS injected on every page load
    login            — Login screen and sidebar user info
    hr_portal        — HR Manager portal (policy, jobs, agent chat tabs)
    candidate_portal — Candidate careers portal (browse & apply)
    employee_portal  — Employee policy Q&A chatbot
"""
